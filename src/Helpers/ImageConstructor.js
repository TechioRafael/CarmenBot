const Jimp = require('jimp');

const imageConstructor = {};

imageConstructor.guildMemberAdd = (imageUrl) => {
    try {
        // Some image stuff here
    } catch (error) {
        console.error(`ERROR trying make a guilde Member Add image. Error: `, error);
    }
}

module.exports = imageConstructor;